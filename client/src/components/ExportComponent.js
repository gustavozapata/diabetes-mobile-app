import React, { useContext } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableHighlight,
} from "react-native";
import AppContext from "../context/AppContext";

const ExportComponent = ({navigation, route}) => {
  
  return (
    <View>
      <Text>Export</Text>
      
    </View>
  );
}

export default ExportComponent;